import React from "react";
import classes from "./AdditionalInfo.css";

const AdditionalInfo = props => (
  <div>A text box that will allow users to type requests / data to us</div>
);

export default AdditionalInfo;
